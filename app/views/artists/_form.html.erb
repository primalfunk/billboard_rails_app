

<%= form_for @artist do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :rating %>
  <%= f.text_field :rating %>

  <%= f.label :review %>
  <%= f.text_area :review %>

  <%= f.submit 'Enter Review'%>
<% end %>